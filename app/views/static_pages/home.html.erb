<% if signed_in? %>
  <div class="row">
  	<aside class="span4">
  		<section>
  			<%= render 'shared/user_info' %>
  		</section>
  		<section>
 			<%= render 'shared/micropost_form'%>
  		</section>
  		<section>
  		     <%= link_to "Emergency Response Situation Map", situation_geosmaps_path, class: "btn btn-large btn-warning"%>
        </section>
  	</aside>
    <div class="span8">
    	<h3>Operations Feed</h3>
    	<%= render 'shared/feed' %>
    </div>
  </div>
<% else %>

  <div class="center hero-unit">
     <h2>Welcome</h2>
     <h2> 
	  <a href="http://idea-er.com/">IDea Emergency Response C2 System</a>
		
     </h2>

     <%= link_to "Show Emergency Response Situation Map", situation_geosmaps_path, class: "btn btn-large btn-warning"%>
   </div>

    <%= link_to image_tag("rails.png", alt: "Rails"), 'http://rubyonrails.org/'%>

<% end %>
